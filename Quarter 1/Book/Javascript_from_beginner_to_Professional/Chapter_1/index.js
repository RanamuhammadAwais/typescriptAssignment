"use strict";
/* git remote add any name gitidentifier
to push
git remote set-url origin git i.d with gitidentifier

then push
git push origin main

*/
//we can add javascripot file into html file by using <script> 
//prompt is used to get input from user it is like alert
prompt("hi , how are you today ?");
// Random number , it generate random number between 0 and 1
Math.random();
console.log(Math.floor(Math.random() * 100));
